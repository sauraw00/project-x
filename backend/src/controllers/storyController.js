export const getStories = async (_req, res) => {
  res.status(501).json({ message: "Stories endpoint pending implementation" });
};

export const getStoryById = async (_req, res) => {
  res.status(501).json({ message: "Single story endpoint pending implementation" });
};

export const toggleBookmark = async (_req, res) => {
  res.status(501).json({ message: "Bookmark endpoint pending implementation" });
};

export const scrapeStories = async (_req, res) => {
  res.status(501).json({ message: "Scrape endpoint pending implementation" });
};
